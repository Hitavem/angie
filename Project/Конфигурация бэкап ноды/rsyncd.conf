[angie] # Директория с конфигурацией Angie
    path = /etc/angie
# Пользователь для синхронизации
    uid = angie-ha-sync
# Группа пользователя
    gid = angie-ha-sync
# IP или подсеть, с которой разрешено подключение
    hosts allow = 10.31.78.0/24
# Запретить все остальные
    hosts deny = *
